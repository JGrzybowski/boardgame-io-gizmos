test.todo(
  "Resets player's energy storage state to the state before build started"
);

test.todo("Removes card from build slot");
test.todo("Reoves card cost from build card cost");

test.todo("Returns card to it's space on the table if the build is from table");
test.todo(
  "Returns card to it's space on the researched if the build is from researched"
);
test.todo(
  "Returns card to it's space on the archived if the build is from archive"
);

test.todo("can't be undone");
